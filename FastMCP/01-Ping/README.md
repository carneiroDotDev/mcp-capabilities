# 01-Ping (FastMCP)

A simple FastMCP stdio-based server that responds to `ping` requests.

Run the server:

```bash
# From FastMCP/
uv run python 01-Ping/src/index.py
```

Run the test client:

```bash
uv run python 01-Ping/src/test_client.py
```
